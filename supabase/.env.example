# Local Edge Function flags (copy to supabase/.env if you use `supabase functions serve`)
ALLOW_DEV_GRANTS=true
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
REVENUECAT_WEBHOOK_AUTHORIZATION=
# Optional but recommended after HMAC signing is enabled in RevenueCat.
REVENUECAT_WEBHOOK_SIGNING_SECRET=
